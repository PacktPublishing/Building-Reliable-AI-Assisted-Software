# Chapter 1 - The Reliability Gap
# Author: Imran Ahmad
#
# Deliberately minimal: the chapter argues from architecture, and its one
# program needs nothing beyond the standard library plus a chart.
matplotlib>=3.8,<4.0
python-dotenv>=1.0,<2.0
colorama>=0.4.6,<0.5
jupyter>=1.0
ipykernel>=6.29,<7.0
pytest>=8.0,<9.0

# Optional - only if you want Listing 1.1 to call a real model:
# openai>=1.40,<3.0
